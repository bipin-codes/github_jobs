import React from "react";
const Loader = () => (
  <div
    style={{
      minHeight: "100vh",
      width: "100%",
      textAlign: "center",
    }}
  >
    <h1>Loading Jobs ...</h1>
  </div>
);

export default Loader;
